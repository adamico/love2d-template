-- conf.lua
-- LÖVE configuration file

love.conf = function(t)
  t.console = true        -- Enable the debug console for Windows.
  t.window.width = 640    -- Game's screen width (number of pixels)
  t.window.height = 360   -- Game's screen height (number of pixels)
end
